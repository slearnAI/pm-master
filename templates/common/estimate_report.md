# Effort Calibration Report · estimator-agent

> Project: **{{ project.name }}** · divergence threshold = {{ divergence*100 }}% · basis floor = {{ basis_floor }} person-days
> Generated by `scripts/estimator.py` after expert decomposition. Independent second opinion on leaf effort.

## Summary
{{#each findings}}
- {{ this }}
{{/each}}

## Per-Leaf Calibration
| ID | Role | Original | Calibrated | Method | Confidence | Source | Flag | Note |
|----|------|----------|------------|--------|------------|--------|------|------|
{{#each results}}
| {{this.id}} | {{this.role}} | {{this.original_effort}} | {{this.expected_effort}} | {{this.method}} | {{this.confidence}} | {{this.source}} | {{this.flag}} | {{this.note}} |
{{/each}}

## Notes
- `recalibrated` — calibrated effort diverged >{{ divergence*100 }}% from the decomposer's original estimate; calibrated value adopted.
- `split-needed` — calibrated effort exceeds the granularity threshold; do NOT inflate — re-dispatch the domain expert to decompose further.
- `low` confidence — no method/history support; supply `estimate_basis` / scale data or analogous anchor before relying on the number.
- Calibration factors come from the shared `references/estimate-calibration.yaml`, maintained by `calibrate_estimates.py --global` on leaf closeout.
