# {{ view_label }} Schedule & Gantt View · {{ project.name }}

> The Schedule / Gantt plan orchestrates task sequencing and dependencies based on the WBS, marking durations, start/end dates, and milestones to form a trackable project time baseline.

| ID | Task | Duration (days) | Predecessor Dependencies | Start | End | Milestone |
|----|------|----------|----------|------|------|--------|
{{#each tasks}}
| {{this.id}} | {{this.name}} | {{this.duration}} | {{this.dep_label}} | {{this.start}} | {{this.end}} | {{#if this.milestone}}Yes{{else}}No{{/if}} |
{{/each}}

{{#if (eq granularity "fortnight")}}
## Fortnight (2-Week) Plan

| Fortnight | Window | # Tasks | Work Packages |
|-----------|--------|---------|---------------|
{{#each fortnight_groups}}
| {{this.label}} | {{this.start}} → {{this.end}} | {{this.count}} | {{this.task_ids}} |
{{/each}}
{{/if}}

## Milestone Coverage (Pillar 2 · 里程碑↔活动归集)

> 每个里程碑下直接归属的活动（按叶子 `milestone_ref` 或依赖末端落在里程碑上）。
> 一个支付里程碑可对应多个活动；支付顺序见下节 Payment Linkage。

| Milestone | Payment ID | Scheduled | # Activities | Activities |
|-----------|------------|-----------|-------------|------------|
{{#each milestone_coverage}}
| {{this.id}} {{this.name}} | {{this.payment_id}} | {{this.date}} | {{this.leaf_count}} | {{this.leaves}} |
{{/each}}

## Payment Linkage (Pillar 4 · 支付里程碑↔排期联动)

> 收款节奏须等于交付节奏：每条固定费率支付行 → 其里程碑 → 排期日 vs 合同 due_date。
> 一致=linked；排期晚于合同 due=drift（须纠偏或走变更）；无合同 due=no-due（须补合同日期）。

| SOW | Payment | Milestone | Scheduled | Contract Due | Fee | Status |
|-----|---------|-----------|-----------|-------------|-----|--------|
{{#each payment_linkage}}
| {{this.sow}} | {{this.payment_id}} | {{this.milestone}} | {{this.scheduled}} | {{this.contract_due}} | {{this.fee}} | {{this.status}} |
{{/each}}

## Gantt Chart

```mermaid
{{#if (eq granularity "fortnight")}}gantt
    title {{ project.name }} · {{ view_label }} Schedule Gantt (Fortnight)
    dateFormat YYYY-MM-DD
    axisFormat %m-%d
{{#each fortnight_groups}}
    section {{this.label}}
{{#each this.tasks}}
{{#unless this.is_summary}}
{{#if this.milestone}}
    {{ gname(this.name) }} :milestone, {{this.start}}, {{this.end}}
{{else}}
    {{ gname(this.name) }} :{{ mid(this.id) }}, {{this.start}}, {{this.end}}
{{/if}}
{{/unless}}
{{/each}}
{{/each}}
{{else}}gantt
    title {{ project.name }} · {{ view_label }} Schedule Gantt
    dateFormat YYYY-MM-DD
    axisFormat %m-%d
{{#each tasks}}
{{#unless this.is_summary}}
{{#if this.milestone}}
    {{ gname(this.name) }} :milestone, {{this.start}}, {{this.end}}
{{else}}
    {{ gname(this.name) }} :{{ mid(this.id) }}, {{this.start}}, {{this.end}}
{{/if}}
{{/unless}}
{{/each}}
{{/if}}
```

_Generated by PM Master · Schedule Gantt Template_
